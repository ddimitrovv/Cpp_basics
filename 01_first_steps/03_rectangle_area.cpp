#include <iostream>
using namespace std;

int main() {
        int length, width;

        cin >> length;
        cin >> width;

        int area = length * width;
        cout << area << endl;

        return 0;
}